

function createBoardState()
	local size = board.getBoardSize()
	unity.print(size)
	local boardArr = {}
	for x = 1, size do
		boardArr[x] = {}
		for y = 1, size do
			local state = board.getTileState(x, y)
			--unity.print(state)
			boardArr[x][y] = state
		end
	end
	return boardArr
end